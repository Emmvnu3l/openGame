package shared;

public class NetworkConstants {
    // Puerto donde el servidor escuchará y el cliente se conectará
    public static final int PORT = 5555;
    // Dirección del servidor (localhost para pruebas locales)
    public static final String HOST = "localhost";
}